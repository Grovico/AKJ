[build]
  command = "npm run build:client"
  publish = "dist/public"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# SPA: all routes -> index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
